/* Real payment request flow. Never grants premium access client-side. */
(function(){'use strict';
try{localStorage.removeItem('smc_vip');localStorage.removeItem('smc_premium_key');localStorage.removeItem('smc_premium_plan');localStorage.removeItem('smc_device_bind');}catch(e){}
var cfg=window.SMC_FIREBASE_CONFIG||{};
function load(src){return new Promise(function(ok,no){var s=document.createElement('script');s.src=src;s.onload=ok;s.onerror=no;document.head.appendChild(s);});}
function boot(){var p=Promise.resolve();if(!window.firebase||!firebase.database)p=p.then(function(){return load('https://www.gstatic.com/firebasejs/10.12.5/firebase-database-compat.js');});return p.then(function(){if(!firebase.apps.length)firebase.initializeApp(cfg);});}
window.unlockVIP=function(){alert('VIP codes are disabled. Paid access is controlled by Firebase approval.');return false;};
function selected(){var cards=document.querySelectorAll('.plan'),idx=0;cards.forEach(function(c,i){if(c.classList.contains('selected'))idx=i;});return idx===1?{price:49,plan:'premium49',label:'Premium ₹49'}:{price:99,plan:'premium99',label:'Premium ₹99'};}
function submitRequest(){boot().then(function(){var user=firebase.auth().currentUser;if(!user){location.replace('login.html');return;}var plan=selected();var name=((document.getElementById('userName')||{}).value||user.displayName||'').trim();var phone=((document.getElementById('userPhone')||{}).value||'').replace(/\D/g,'');var email=((document.getElementById('userEmail')||{}).value||user.email||'').trim();var txn=((document.getElementById('txnId')||{}).value||'').trim();var amount=parseInt((document.getElementById('txnAmount')||{}).value||'0',10);if(name.length<2)return alert('Enter your full name.');if(!/^\d{10}$/.test(phone))return alert('Enter a valid 10-digit mobile number.');if(txn.length<6)return alert('Enter the UPI transaction/UTR number.');if(amount!==plan.price)return alert('Entered amount must match ₹'+plan.price+'.');var order='SMC-'+Date.now().toString(36).toUpperCase()+'-'+Math.random().toString(36).slice(2,7).toUpperCase();var payload={orderId:order,uid:user.uid,email:user.email||email,name:name,phone:phone,plan:plan.plan,planLabel:plan.label,amount:plan.price,txnId:txn,status:'pending',createdAt:new Date().toISOString(),source:'website'};var btn=document.getElementById('finalVerifyBtn');if(btn){btn.disabled=true;btn.textContent='Submitting…';}return firebase.database().ref('payment_requests/'+order).set(payload).then(function(){var key=document.getElementById('accessKey');if(key)key.textContent='PENDING APPROVAL';var kr=document.getElementById('keyResult');if(kr)kr.style.display='block';var vb=document.getElementById('verifyPayBtn');if(vb)vb.style.display='none';alert('Payment submitted. ₹'+plan.price+' access will activate only after admin verifies the UPI transaction.');});}).catch(function(e){var b=document.getElementById('finalVerifyBtn');if(b){b.disabled=false;b.textContent='Submit Payment for Verification';}alert(e.message||'Unable to submit payment request.');});}
window.verifyPayment=submitRequest;
})();
